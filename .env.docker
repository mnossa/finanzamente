# Docker environment variables
# Imposta UID e GID per evitare problemi di permessi
UID=1000
GID=1001

# Override solo per la rete Docker (il .env montato resta la base)
PYTHON_SERVICES_URL=http://python-services:8000
